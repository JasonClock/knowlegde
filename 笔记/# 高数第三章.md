# 高数第三章

## 函数的单调性
函数拐点是坐标
函数的极值和最值是x的值

### 单调性的判别法
if 在（a,b）内f'~(x)~>0,则f~(x)~在[a,b]上单调增加
（用==微分中值定理==证明）
证：f~(x1)~ - f~(x2)~ = f'~(m)~(x~1~-x~2~)
x~1~-x~2~>>0
m在(a.b)上f'(m)>0
所以f(~x1~)-f(~x2~)>0
:
驻点是f'(x)=0的点
折点是f(x)的一阶导不存在
上面的两点可能是单调区间的分界点

